# Gap Analysis (Phase 1)

_Empirical record, intent/reality delta — the [documentation model](./README.md) maps every document's role._

Every gap the six-area audit found, grouped by kind, with the action **recorded —
not implemented**. Evidence is file:line or command output, verifiable from the
repository — the citations in each row are self-contained. Items already resolved inside
PR #237 are listed under Correct with their resolution, so nothing silently
disappears. One defect was severe enough to file immediately:
[#238](https://github.com/ecoma-io/loom/issues/238) (tree-view manifest missing
`"private": true`).

## Correct

| #   | Finding                                                                                                                                                                                                                            | Evidence                                                                                | Action / resolution                                                                                                                                                               |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Primitive count drift: model said "~80", actual is 76                                                                                                                                                                              | `printf '%s\n' packages/primitives/*/ \| wc -l` → 76                                    | **Resolved in PR #237** (`445cc92`)                                                                                                                                               |
| C2  | Mapping table did not flag DashboardGrid beside DesktopAppShell                                                                                                                                                                    | `DashboardGrid.vue:3-19`                                                                | **Resolved in PR #237** (`445cc92`); decision itself is S6                                                                                                                        |
| C3  | Status rows in interface-contract overstated enforcement (JS side effects "no gate"; composition "no gate" ignoring the conformance route; five-artifact attribution)                                                              | semgrep `memory-leaks.yml:54-74,211-232`; `conformance.ts:81-86`; `ci.yml` Verify steps | **Resolved in PR #237** (final commit)                                                                                                                                            |
| C4  | Wrong-direction dependency example in contract.md rule 2                                                                                                                                                                           | `contract.md:96-98` pre-fix                                                             | **Resolved in PR #237** (`08deed5`)                                                                                                                                               |
| C5  | Comment drift: "all 94 demos" ×3 in `a11y-scope.ts`; actual 109 (no coverage hole — the sweep globs the directory)                                                                                                                 | `a11y-scope.ts:50,76,205`                                                               | **Resolved in the Phase 0.5 PR** — comments reworded count-free (the sweep globs the directory; a count in a comment goes stale by construction)                                  |
| C6  | Stale claims: `docs/moon.yml:101-102` ("only unit test is sidebar.test.ts"); `tree-view/moon.yml:3` advertises an unused `# preserved` edge; `e2e/layout-responsive.e2e.ts:3-11` header names components the tests no longer drive | file:lines as listed                                                                    | **Resolved in the Phase 0.5 PR** — comments now describe the tree they live in                                                                                                    |
| C7  | README template names stale ("AI Chat, Dashboard, Music Player"); npm description and docs landing still frame Loom as cross-platform                                                                                              | `README.md:79`; `package.json` description; `docs/index`                                | **Decided, not an action item**: the constitution records these as conflicts those surfaces' own changes resolve; riding them into PR 3 would broaden a docs PR against its scope |

## Terminology mismatch

| #   | Finding                                                                                                                                                                                                                                                                                     | Evidence                                                                                        | Action                                                                                                                            | Phase                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| T1  | "block" names the Pattern kind on four surfaces beyond the directory: gate tier noun (`check-component-artifacts.ts:31`), `layer-blocks` rows + chain (`module-boundaries.config.mjs:8,123-137`), `docs/blocks/` (15 pages), component docstrings (`TitleBar.vue:3`, `AppHeader.vue:16,33`) | as listed                                                                                       | Build the exhaustive rename inventory from these surfaces when the seven-kind migration decision lands                            | Phase 2 (with S1)    |
| T2  | "Component" carries three meanings: docs section name for the Primitive tier, the #218 umbrella term, the facade's all-tiers usage                                                                                                                                                          | `check-component-artifacts.ts:29`; `artifact-model.md:198`; `index.ts:4-5`                      | **Resolved in the Phase 0.5 PR** — the artifact model's terminology status table now carries all three meanings with their status | Resolved (Phase 0.5) |
| T3  | Dependency-direction prose omits layout-engine in both readers while every table places it and the docs call it a foundation                                                                                                                                                                | `check-architecture.ts` DIRECTION; `module-boundaries.config.mjs:8`; `.moon/workspace.yml:8-12` | **Resolved in the Phase 2 PR** — both readers now state one canonical order sentence including the engine                         | Resolved (Phase 2)   |

## Structural mismatch

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                              | Evidence                                                                                                          | Action                                                                                                                                                                                                                                                                                                                                            | Phase                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| S1  | blocks tier sits at rank 5 above layouts while the model places Pattern below Layout — the machine chain is the inverse of the model's                                                                                                                                                                                                                                               | `module-boundaries.config.mjs:8` vs `artifact-model.md:98,107-110`                                                | **Resolved in the Phase 2 PR** — ranks inverted (blocks/Pattern rank 4, layouts 5) in both readers + mutation suite; the blocks→patterns rename lands 2C                                                                                                                                                                                          | Resolved (Phase 2)              |
| S2  | DesktopAppShell is a Layout by the model's own clauses (purpose, may-own, must-not-own, deps all match; deps set exactly Layout's expected set)                                                                                                                                                                                                                                      | `DesktopAppShell.vue:3-6,81-118`; `package.json:12-17`; `artifact-model.md:112-121`                               | **Decided in the Phase 2 PR (ADR-001)** — reclassified Layout; lands in `packages/layouts/` during 2C                                                                                                                                                                                                                                             | Decided (ADR-001), migration 2C |
| S3  | DashboardGrid reads as Composition (arrangement-only, container-driven, presentational); Composition-vs-Layout undecided                                                                                                                                                                                                                                                             | `DashboardGrid.vue:3-19`                                                                                          | **Decided in the Phase 2 PR (ADR-001)** — reclassified Composition; lands in `packages/composition/` during 2C                                                                                                                                                                                                                                    | Decided (ADR-001), migration 2C |
| S4  | TitleBar is a borderline-but-defensible Pattern (window-chrome region)                                                                                                                                                                                                                                                                                                               | `TitleBar.vue:12-15`                                                                                              | **Decided in the Phase 2 PR (ADR-001)** — records the judgement; lands in the renamed `packages/patterns/` during 2C                                                                                                                                                                                                                              | Decided (ADR-001), migration 2C |
| S5  | Facade trims 7 package-index exports while siblings export theirs; no rule states which trims are deliberate; contradicts contract.md:124-125's "every component package's public surface" reading                                                                                                                                                                                   | facade vs package-index diff, four indexes                                                                        | **Closed** — 2E decided per name: `COPY_BUTTON_LABELS` and `timelineMarkerVariants` added to the facade (label/variant maps are public surface); `COPY_REVERT_MS`, `buttonVariantClasses`, `TableRowState`, `headAlignClass`, `nextSort` stay internal helpers, stated in `packages/loom/src/index.ts` and watched by `check-api-parity.ts` (M12) | Done                            |
| S6  | `./theme` wired for publish but unwired in the workspace: missing from the facade package exports, root vite aliases and docs aliases; tsconfig maps it to `core/src/theme.ts`, not the built `loom/src/theme.ts`. (Audit correction: the published root exports map **does** carry `./theme` — `package.json:53-56` — so real consumers are wired; this is workspace-internal only) | `package.json:53-56`; `vite.config.ts:176 vs :42,166`; `tsconfig.base.json:87`; `packages/loom/package.json:5-14` | **Closed** — 2E added `@ecoma-io/loom/theme` to the vite config, docs config and facade `package.json` exports, and pointed the tsconfig path at `packages/loom/src/theme.ts`; `check-api-parity.ts` (M12) now asserts the exports↔build↔declarations set                                                                                         | Done                            |

## Dependency violation

| #   | Finding                                                                                                                                                                   | Evidence                                                                                             | Action                                                                                                                                                                                                          | Phase              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| D1  | Machine rank permits the model-forbidden Pattern→Layout edge (`layer-blocks` row allows `layer-layouts`); zero blocks exercise it today                                   | `module-boundaries.config.mjs` layer-blocks row; bounded grep → 0 imports                            | **Resolved in the Phase 2 PR** — the row now forbids it (blocks may not reach layouts); mutation `pattern-imports-layout` pins it                                                                               | Resolved (Phase 2) |
| D2  | `@internationalized/date` declared in root dependencies but not externalised — bundled into dist while consumers also install it (dual copy alongside reka-ui's own copy) | `package.json:133`; `vite.config.ts:183-194` (bundling inferred — dist not in tree)                  | **Closed** — 2E added `@internationalized/date` to the facade build's rollup `external` list; it stays a root dependency and the date packages' own dependencies, so the facade no longer bundles a second copy | Done               |
| D3  | Layout-engine edge legality diverges between readers (text reader rank 0 passes; rows forbid) — listed here because it is a would-be violation caught by one reader only  | `check-architecture.ts` engine-edge judgment; `module-boundaries.config.mjs` layer-layout-engine row | **Resolved in the Phase 2 PR** — text reader now judges the engine by its real consumer set (compositions only); checker fixtures pin both faces                                                                | Resolved (Phase 2) |

## API boundary issue

| #   | Finding                                                                                                                                                                                           | Evidence                                                                               | Action                                                                                                                                                                                                 | Phase     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- |
| A1  | `@ecoma-io/loom-tree-view` is publishable-shaped: no `"private"` field, exports→src, workspace deps, vue peer — the only one of 107                                                               | `packages/primitives/tree-view/package.json`; 75/76 siblings private                   | **Filed as [#238](https://github.com/ecoma-io/loom/issues/238)**; fix is one line plus a gate (with A2)                                                                                                | Immediate |
| A2  | No gate asserts component-manifest privacy, while templates get exactly that gate                                                                                                                 | `check-template-artifacts.ts:44-45` vs `check-component-artifacts.ts` (manifest-blind) | **Closed** — `check-manifest-privacy.ts` (2D) asserts `private` + no `publishConfig` across `packages/**/package.json`, both one-level and two-level; #238 fixed                                       | Done      |
| A3  | Two exported components have no `@api` marker anywhere — `ToolbarSeparator`, `ToastItem`; their props render nowhere; the marker gate keys on the primary component name only                     | marker grep; `check-component-artifacts.ts:110-114`                                    | **Closed** — 2E added `<!-- @api ToolbarSeparator -->` to `docs/components/toolbar.md` and `<!-- @api ToastItem -->` to `docs/components/toast.md`; their props now render in the generated API tables | Done      |
| A4  | `@lucide/vue` imported by 20 demos, declared only as a root dependency — a consumer copying a demo imports a package the demo never declares, and under strict layouts (pnpm) it will not resolve | demo grep; `package.json:134`                                                          | **Closed** — 2F declared `@lucide/vue` ^1.40.0 wherever Loom's code imports it (core, collapse, sidebar-nav added; 25 ranges normalized); the consumer note lives on the iconography page              | Done      |
| A5  | Internal-seam exports published without stated rationale (`warningWindow`, `useOuterFieldContext`, motion plumbing); type-surface curation ungoverned                                             | facade core/labels sections; `contract.md:45-46`                                       | One-line rationale per export class in the interface contract, or trim                                                                                                                                 | Defer     |
| A6  | Default-export inconsistency among multi-component packages (list/toast none; table/toolbar/timeline yes)                                                                                         | `index.ts:127,269` vs `:142-152,267`                                                   | Pick one convention at the next facade change                                                                                                                                                          | Defer     |

## Consumer-boundary issue

| #   | Finding                                                                                                                                                                     | Evidence                                                                       | Action                                                                                                                                                                                                                 | Phase |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| B1  | Dead VitePress alias map: ~109 `@ecoma-io/loom-*` entries no docs file imports (migration completed per `docs/architecture/contract.md`; stale in-progress comment remains) | `config.mts`; grep → zero usages                                               | **Closed** — 2F pruned the map to the three facade keys (#256) and replaced the stale comment with why nothing else belongs there                                                                                      | Done  |
| B2  | Moon e2e task enumerates six of seven root specs by hand — `reduced-motion.e2e.ts` only reached via Playwright testMatch, invisible to the moon path                        | `e2e/moon.yml`; `playwright.config.ts:41`                                      | **Closed** — 2F deleted the args; the resolved task now runs exactly `playwright test --config playwright.config.ts`, so the suite is whatever the config's filesystem collection (`testMatch` + `testIgnore`) gathers | Done  |
| B3  | Conformance route: licensed, disclosed, mutation-guarded — no action. Recorded here so the boundary section is complete                                                     | `module-boundaries.config.mjs:210-211`; suppressions; 7-hop trace in the audit | —                                                                                                                                                                                                                      | —     |

## Missing enforcement

| #   | Finding                                                                                                                                                                                                                                     | Evidence                                                               | Action                                                                                                                                                                                                                       | Phase              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| M1  | Engine-rank reader divergence (see D3): one reader passes a would-be violation the other fails                                                                                                                                              | as D3                                                                  | **Resolved in the Phase 2 PR** — text reader's engine judgment matches the boundary rows; checker fixtures pin the engine case                                                                                               | Resolved (Phase 2) |
| M2  | Check 2's facade regex misses dashed subpaths and bare side-effect imports (internal regex already `[\w-]+`); latent — only `\w+`-shaped subpaths exist                                                                                     | `check-architecture.ts:95,186`                                         | Widen the group; add bare-import alternative; fixtures for both                                                                                                                                                              | Phase 2            |
| M3  | `tools/` has 3 real cross-project reads absent from the moon affected graph (`stage-docs.ts:30` → docs base; `e2e-plan.ts:77,81` → playwright/e2e)                                                                                          | file:line; `tools/moon.yml` has no deps block                          | **Closed** — 2F hand-declared all three under `# preserved` markers (docs, playwright, e2e); `moon query projects` reports them explicitly                                                                                   | Done               |
| M4  | `sync-moon-deps` scope excludes fixed packages; the facade's 109 moon deps are hand-maintained and a new import's missing moon entry is caught by nothing until affected silently drops it                                                  | `sync-moon-deps.ts:32,44-64`; `loom/moon.yml:6-10`                     | **Closed** — 2F widened the sync to every `packages/` directory carrying a package.json; labels gained `core` and the facade aligned to its 109 manifest entries                                                             | Done               |
| M5  | "Zero engine bytes in dist" is recorded prose, not a standing check; a barrel re-export chain would pass both readers today                                                                                                                 | `contract.md:364-366`; `index.ts:7-10`; config passes re-export chains | Cheap standing check: fail on any re-export of `./layout` below facade; or build-then-grep-dist in CI; add a mutation row                                                                                                    | Phase 2            |
| M6  | No gate forces adapter/case coverage: a new composition can land with neither `src/layout.ts` nor conformance cases                                                                                                                         | `conformance.ts:81-86` lists four modules by hand                      | Gate mirroring check-component-artifacts: every composition owns adapter+cases or a recorded exception                                                                                                                       | Phase 2            |
| M7  | No gate enumerates the required conformance case matrix — a new breakpoint value or Frame ratio could ship untested CSS with every gate green                                                                                               | adapter type records + band self-check cover only part                 | Coverage-floor tests assert every prop value appears in ≥1 case                                                                                                                                                              | Phase 2 (with M6)  |
| M8  | Docs `WCAG_TAGS` transcription unpinned (currently byte-identical; zero tooling references; only the 5-tag array is transcribed)                                                                                                            | `docs/foundations/accessibility.md:12-15`                              | Five-line vitest pin: fence equals JSON of `WCAG_TAGS`                                                                                                                                                                       | Phase 2            |
| M9  | Token-usage / hard-coded visual values unlinted (no stylelint, no semgrep CSS rules, no custom check)                                                                                                                                       | full eslint/semgrep config inspection                                  | stylelint declaration allowlist sourced from theme-core tokens                                                                                                                                                               | Phase 2            |
| M10 | Import-time-browser-code contract only partially gated: semgrep leak rules cover leak-shaped side effects; import-time surface is review-held                                                                                               | `memory-leaks.yml:54-74,211-232`; `analysis.yml:132-135`               | Extend semgrep rules + scoped eslint rule; relabel per audit                                                                                                                                                                 | Phase 2            |
| M11 | Template artifact gate (`check-template-artifacts.ts`) runs only in root `pnpm lint`; CI's lint path is affected moon ESLint tasks that never chain it — **verified** (`grep check-template-artifacts .github/workflows/ci.yml` → no match) | `package.json:74`; `ci.yml` Verify steps                               | **Closed** — 2F added both it and `check-manifest-privacy.ts` as unconditional verify steps beside the other artifact gates                                                                                                  | Done               |
| M12 | No gate asserts exports↔lib.entry↔declarations↔styles parity, though contract.md:138-141 already declares such drift a defect                                                                                                               | `package.json:44-61`; `vite.config.ts:172-204`                         | **Closed** — 2E added `tools/check-api-parity.ts`, wired into `pnpm lint` and CI, asserting the exports map, vite `lib.entry` keys, `packages/loom/src` declarations, `./styles/*.css` sources and docs references all agree | Done               |
| M13 | Responsive evidence: 3/8 layouts, 3/8 compositions and all blocks lack browser gates; mobile profiles only on infra legs                                                                                                                    | `e2e/layout-responsive.e2e.ts:34-182`; `e2e-plan.ts:479`               | Structural gate: every layout owns a viewport spec or conformance case; registry-driven enumeration                                                                                                                          | Phase 2            |
| M14 | Interaction evidence: 44/76 primitives own no harness spec; ownership rule is one-directional (specs→tag)                                                                                                                                   | `check-architecture.ts:109-126`; 32/76 e2e dirs                        | Role-driven gate: interactive-ARIA-role primitives own an interaction spec                                                                                                                                                   | Phase 2            |

## Product-scope risk

| #   | Finding                                                                                                                                                                                                | Evidence                                                                                | Action                                                                                                                                                                  | Phase   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| P1  | 4/8 compositions have no computable twin; grid/sidebar/scroll-reel have jsdom class pins only, split one behavioural e2e — the engine's coverage is a minority of the family it mirrors                | engine-audit F8 table; `artifact-model.md:88` promises per-composition responsive gates | Adapters in risk order sidebar → grid → split (percent boundary first); declare scroll-reel CSS-only in writing if that is the decision; boundingBox smoke as the floor | Phase 2 |
| P2  | The engine models a single-line flex subset (no wrap/reverse/baseline/percent/text metrics) — correct today, but nothing records the subset as the declared scope beside the adapters that throw on it | `src/layout.ts` semantics; adapter throws                                               | Record the modelled subset in the artifact model's engine section                                                                                                       | Phase 2 |
| P3  | Per-component interaction depth is the conceded "varies" while decided interaction is the library's core promise                                                                                       | interface-contract status row; audit §Semantic interaction                              | Same gate as M14; treat depth as roadmap, not gate                                                                                                                      | Phase 2 |
| P4  | Docs demo snippets depend on `@lucide/vue` undeclared in the demos consumers copy (same fact as A4, consumer dimension)                                                                                | as A4                                                                                   | **Closed** with A4 — one note on the iconography page: a copied demo's `@lucide/vue` import resolves only through the copier's own manifest                             | Done    |

## Disposition of PR 3

The originally-suspected "required consistency fixes" (README template names, npm
description framing, adjacent contract.md example) resolved as: the contract.md
example landed in PR #237 (`08deed5`); the README/description items are recorded
conflicts under the constitution's own §1 language, carrying actions for the
surfaces that own them (C7). **No PR 3 is required**; nothing else in this
analysis is a consistency fix — everything above is Phase 2 material, recorded and
not implemented, per the Phase 0–1 stop boundary.
