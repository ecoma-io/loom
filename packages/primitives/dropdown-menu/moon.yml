deps:
  - "core"
tags: ["layer-primitives"]
project:
  name: "dropdown-menu"
  description: "DropdownMenu primitive — button-activated command list"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
