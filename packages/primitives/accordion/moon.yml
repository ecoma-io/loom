deps:
  - "core"
tags: ["layer-primitives"]
project:
  name: "accordion"
  description: "Accordion primitive — vertically collapsing content panels"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
