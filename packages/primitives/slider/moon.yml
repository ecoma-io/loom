deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "slider"
  description: "Slider primitive — continuous bounded value control"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
