tags: ["e2e"]

project:
  name: "menubar"
  description: "Menubar primitive — horizontal menu bar for application-level navigation"
tasks:
  # Inherit build, lint, typecheck and test from .moon/tasks/*.yml.
  e2e:
    # Owned by the component suite (packages/<tier>/<name>/e2e) and by the
    # dedicated matrix job in ci.yml. moon ci must not run it again.
    options:
      runInCI: skip
    args: ["packages/primitives/menubar/e2e"]
    env:
      PW_PROFILE: "smoke"
