deps:
  - "core"
tags: ["layer-primitives"]
project:
  name: "skeleton"
  description: "Skeleton primitive — content-loading placeholder for a known layout"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
