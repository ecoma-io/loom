tags: ["e2e"]

project:
  name: "radio-group"
  description: "RadioGroup primitive — mutually exclusive option list"
tasks:
  # Inherit build, lint, typecheck and test from .moon/tasks/*.yml.
  e2e:
    args: ["packages/primitives/radio-group/e2e"]
    env:
      PW_PROFILE: "smoke"
