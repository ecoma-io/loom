project:
  name: "separator"
  description: "Separator primitive — thin visual/semantic divider between content groups"

# Separator depends only on core (cn). As cross-component dependencies emerge
# (e.g. labels for labeled components), they are added here.
tasks:
  # Inherit build, lint, typecheck and test from .moon/tasks/*.yml.
