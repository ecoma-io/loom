deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "breadcrumb"
  description: "Breadcrumb primitive — hierarchical navigation trail"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
