deps:
  - "core"
  - "labels"
  - "button"
tags: ["layer-primitives"]
project:
  name: "pagination"
  description: "Pagination primitive — paged navigation"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
