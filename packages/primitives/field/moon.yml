deps:
  - "core"
  - "labels"
  - "inline-error"
project:
  name: "field"
  description: "Field primitive — labelled form control wrapper"
tasks:
  # Inherit build, lint, typecheck and test from .moon/tasks/*.yml.
