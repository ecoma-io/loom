deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "otp-input"
  description: "OtpInput primitive — one-time code entry row"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
