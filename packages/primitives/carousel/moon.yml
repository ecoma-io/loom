deps:
  - "core"
  - "icon-button"
  - "labels"
tags: ["layer-primitives", "e2e"]
project:
  name: "carousel"
  description: "Carousel primitive — paged strip with prev/next controls"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
  e2e:
    options:
      runInCI: skip
    args: ["packages/primitives/carousel/e2e"]
    env:
      PW_PROFILE: "smoke"
