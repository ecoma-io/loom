deps:
  - "core"
  - "labels"
project:
  name: "text-field"
  description: "TextField primitive — single-line text entry with reveal and counting"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
