deps:
  - "core"
  - "labels"
  - "button"
tags: ["layer-primitives"]
project:
  name: "editable"
  description: "Editable primitive — inline text editing"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
