deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "select"
  description: "Select primitive — dropdown single-selection control"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
