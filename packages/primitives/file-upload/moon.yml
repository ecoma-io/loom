deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "file-upload"
  description: "FileUpload primitive — file selection with validation"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
