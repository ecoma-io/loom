deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "date-time-picker"
  description: "DateTimePicker primitive — date and time selection"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
