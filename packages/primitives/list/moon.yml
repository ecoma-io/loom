deps:
  - "core"
tags: ["layer-primitives", "e2e"]
project:
  name: "list"
  description: "List primitive — titled rows with leading and trailing content"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
  e2e:
    options:
      runInCI: skip
    args: ["packages/primitives/list/e2e"]
    env:
      PW_PROFILE: "smoke"
