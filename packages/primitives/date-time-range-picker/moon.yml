deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "date-time-range-picker"
  description: "DateTimeRangePicker primitive — combined date+time range selection"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
