deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "color-picker"
  description: "ColorPicker primitive — colour selection as #rrggbb"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
