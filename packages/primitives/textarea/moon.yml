deps:
  - "core"
  - "labels"
project:
  name: "textarea"
  description: "Textarea primitive — multi-line text entry with character counting"
tasks:
  # Inherit build, lint, typecheck and test from .moon/tasks/*.yml.
