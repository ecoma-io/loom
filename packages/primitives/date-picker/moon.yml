deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "date-picker"
  description: "DatePicker primitive — date selection"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
