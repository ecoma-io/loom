deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "number-field"
  description: "NumberField primitive — constrained number input with scrub-drag"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
