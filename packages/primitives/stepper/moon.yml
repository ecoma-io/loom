tags: ["e2e"]

project:
  name: "stepper"
  description: "Stepper primitive — the progress spine of a multi-step flow"
tasks:
  # Inherit build, lint, typecheck and test from .moon/tasks/*.yml.
  e2e:
    args: ["packages/primitives/stepper/e2e"]
    env:
      PW_PROFILE: "smoke"
