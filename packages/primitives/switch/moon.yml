deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "switch"
  description: "Switch primitive — immediate boolean toggle"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
