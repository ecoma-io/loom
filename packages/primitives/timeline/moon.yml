deps:
  - "core"
  - "labels"
tags: ["layer-primitives", "e2e"]
project:
  name: "timeline"
  description: "Timeline primitive — chronological spine of moments"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
  e2e:
    options:
      runInCI: skip
    args: ["packages/primitives/timeline/e2e"]
    env:
      PW_PROFILE: "smoke"
