deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "checkbox"
  description: "Checkbox primitive — binary selection control"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
