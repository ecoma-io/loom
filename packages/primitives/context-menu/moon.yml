deps:
  - "core"
tags: ["layer-primitives"]
project:
  name: "context-menu"
  description: "ContextMenu primitive — right-click activated menu"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
