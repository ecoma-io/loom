deps:
  - "core"
tags: ["layer-primitives"]
project:
  name: "popover"
  description: "Popover primitive — floating content panel anchored to a trigger"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
