deps:
  - "core"
tags: ["layer-primitives"]
project:
  name: "inline-error"
  description: "InlineError primitive — compact validation message"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
