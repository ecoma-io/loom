deps:
  - "core"
  - "labels"
  - "inline-error"
tags: ["layer-primitives"]
project:
  name: "fieldset"
  description: "Fieldset primitive — grouped form fields with legend"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
