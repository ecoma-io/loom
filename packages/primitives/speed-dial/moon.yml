deps:
  - "core"
tags: ["layer-primitives"]
project:
  name: "speed-dial"
  description: "SpeedDial primitive — expanding fan of quick actions"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
