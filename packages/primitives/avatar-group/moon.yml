deps:
  - "core"
  - "labels"
  - "avatar"
tags: ["layer-primitives"]
project:
  name: "avatar-group"
  description: "AvatarGroup primitive — stacked or overlapping avatar list"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
