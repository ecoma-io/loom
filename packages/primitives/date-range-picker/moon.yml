deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "date-range-picker"
  description: "DateRangePicker primitive — date range selection"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
