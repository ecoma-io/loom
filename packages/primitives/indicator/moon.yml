deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "indicator"
  description: "Indicator primitive — a presence dot or count pill pinned onto content"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
