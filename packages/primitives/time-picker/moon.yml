deps:
  - "core"
  - "labels"
tags: ["layer-primitives"]
project:
  name: "time-picker"
  description: "TimePicker primitive — time selection"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
