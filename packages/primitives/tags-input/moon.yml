deps:
  - "core"
  - "labels"
  - "chip"
  - "field"
project:
  name: "tags-input"
  description: "TagsInput primitive — multi-value token input"
tasks:
  # Inherit lint and test from .moon/tasks/*.yml.
